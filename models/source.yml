version: 2

sources: 
  - name: src
    description: This is source name
    database: JAFFLE
    schema: RAW
    tables:
      - name: customers
      - name: order_items
      - name: orders
        config:
          # source.table.config.freshness overrides source.config.freshness
          freshness: 
            warn_after:
              count: 24
              period: hour
            error_after:
              count: 48
              period: hour        
          loaded_at_field: ordered_at
      - name: products
      - name: locations
      - name: supplies